
{
  "name": "cashflow365",
  "version": "1.0.0",
  "description": "AI-powered Filipino expense tracker",
  "scripts": {
    "mobile": "cd mobile && npm start",
    "mobile:android": "cd mobile && npm run android",
    "mobile:ios": "cd mobile && npm run ios",
    "backend": "cd backend && uvicorn app.main:app --reload",
    "backend:test": "cd backend && python -m pytest",
    "firebase:serve": "cd firebase && firebase serve",
    "firebase:deploy": "cd firebase && firebase deploy",
    "install:all": "cd mobile && npm install && cd ../backend && pip install -r requirements.txt && cd ../firebase/functions && npm install",
    "dev": "concurrently \"npm run mobile\" \"npm run backend\" \"npm run firebase:serve\"",
    "test": "cd mobile && npm test && cd ../backend && python -m pytest"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}